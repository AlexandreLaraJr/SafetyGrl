import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    containerFacebook: {
        width: '100%',
        height: 100,
        borderRadius: 8,
        flexDirection: 'row',
        alignItems: 'center',
        paddingHorizontal: 50
    },

    containerGoogle: {
        width: '100%',
        height: 100,
        borderRadius: 8,
        //flexDirection: 'row',
        //alignItems: 'center',
        paddingHorizontal: -50
    }
})
